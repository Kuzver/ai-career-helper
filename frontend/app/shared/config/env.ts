// TODO zode-схема и валидация
const authApiBaseUrl = import.meta.env.VITE_AUTH_API_BASE_URL
const authAppId = import.meta.env.VITE_AUTH_APP_ID
const apiBaseUrl = import.meta.env.VITE_API_BASE_URL

export const env = {
  authApiBaseUrl,
  authAppId,
  apiBaseUrl,
}
